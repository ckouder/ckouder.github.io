# Work images

Put each work's images in a folder named after its slug:

```
static/works/<slug>/01.jpg
static/works/<slug>/02.jpg
```

Then reference them from `src/lib/content.ts` as `/works/<slug>/01.jpg` with alt text.
